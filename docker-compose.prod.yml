services:
  backend:
    restart: unless-stopped
    env_file: ./backend/.env
    environment:
      ENV: prod
      COOKIE_SECURE: "true"
      CORS_ORIGINS: "https://riskybusinesses.uk,https://www.riskybusinesses.uk"
      BACKEND_URL: "https://riskybusinesses.uk"
      FRONTEND_URL: "https://riskybusinesses.uk"
      WATCHFILES_FORCE_POLLING: ""        # disable, pas de --reload en prod
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --loop asyncio # ← NO --reload

  frontend:
    restart: unless-stopped
    environment:
      VITE_API_URL: "https://riskybusinesses.uk"

  postgres:
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql/data

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - backend
      - frontend

volumes:
  postgres_data:
  caddy_data:
  caddy_config:
